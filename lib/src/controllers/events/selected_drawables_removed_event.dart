import 'painter_event.dart';

/// An event representing the select object drawable being deleted.
class SelectedDrawablesRemovedEvent extends PainterEvent {
  /// Creates an [SelectedDrawablesRemovedEvent].
  const SelectedDrawablesRemovedEvent();
}
